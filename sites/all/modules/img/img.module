<?php

function recipe_form_recipe_node_form_form_alter(&$form, &$form_state){
  var_dump('this is working');
    $form['image'] = array (
    '#type' => 'textfield',
    '#description' => 'recipe image',
    'title' => t('image'),
  );

  $order = array (
  'image',
  'title',
  'yield units'
);

foreach ($order as $key => $field) {
  $form[$field]['#weight'] = $key;
  }
}
